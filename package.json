{
  "name": "komunify",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --parallel -r dev",
    "build": "bun --parallel -r build",
    "typecheck": "bun --parallel -r typecheck",
    "lint": "bun --parallel -r lint",
    "format": "bun --parallel -r format",
    "contract:test": "cd contracts && cargo test",
    "contract:build": "cd contracts && stellar contract build",
    "contract:bindings": "rm -rf tmp-bindings && stellar contract bindings typescript --wasm contracts/target/wasm32v1-none/release/notes.wasm --output-dir tmp-bindings --overwrite && cp tmp-bindings/src/index.ts packages/contract-client/src/index.ts && rm -rf tmp-bindings && cd packages/contract-client && bun run build"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "prettier": "^3.4.2"
  }
}